# packwiz 分发忽略清单 — 这些文件不进 index.toml,不会下发到客户端
# 它们只存在于 git 仓库(维护工具/脚本),不属于整合包内容

# 维护工具与脚本(玩家从导入包/仓库获取,不通过增量更新下发)
/tools/
/bootstrap/
/server/
/.github/

# 迁移审计报告与文档(维护人参考)
/_missing_update.json
/_side_report.json
/MIGRATION.md
/WORKFLOW.md
/README.md
/docs/
/LICENSE

# GitHub Pages 专用(只影响托管,客户端/服务器不需要)
/.nojekyll

# IDE 文件(JetBrains 打开工作区生成,不进 git 也不进分发)
/.idea/

# 游戏运行时生成的服务器配置(不在原始整合包内,不应下发)
/config/inventoryprofilesnext/新的世界/
/config/inventoryprofilesnext/新村庄/
